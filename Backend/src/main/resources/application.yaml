spring:
  datasource:
    url: jdbc:mysql://localhost:3306/abs_ecommerce
    username: root
    password: Dheeran1212#

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 5MB

server:
  port: 8080